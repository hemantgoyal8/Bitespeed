# Dependencies
# This directory contains all third-party libraries installed by npm/yarn.
# It can be very large and is always regenerated from package.json and package-lock.json.
node_modules/

# TypeScript Compiled Output
# This directory contains the JavaScript files generated by the TypeScript compiler (tsc).
# It's specified as "outDir" in your tsconfig.json.
dist/

# Environment Variables File
# This file typically stores sensitive information like database credentials, API keys, etc., for local development.
# It should NEVER be committed to version control.
# Provide a .env.example file instead if you want to show the structure.
.env
.env.*
!.env.example

# Log files
# General log files and specific npm/yarn debug logs.
*.log
npm-debug.log*
yarn-debug.log* # If you were to use yarn
yarn-error.log* # If you were to use yarn

# Operating System generated files
# Common OS-specific files that don't belong in version control.
.DS_Store
Thumbs.db

# Editor/IDE specific files
# Configuration files for common editors like VS Code.
.vscode/
# .idea/ # For IntelliJ IDEA users

# Other common exclusions for Node projects (optional but good practice)
# Coverage directory used by testing tools like Jest/Istanbul
coverage/

# Build artifacts (if you had a different build process or folder name)
# build/

# Temporary files
*.tmp
*.swp